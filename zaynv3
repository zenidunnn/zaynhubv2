--[[
    ZAYN PANEL — Full WindUI Version (FPS & Performance Optimized)
    + Universal Auto Pick Up (Default Keybind: G)
    + Custom Keybinds: Fly (F | Speed 120), Turbo Dig (V), Boulders ESP (C), Combo Dig+Aim (X)
    + Boulders ESP dengan Name Tags / Label Nama
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ProximityPromptService = game:GetService("ProximityPromptService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local CoreGui = game:GetService("CoreGui")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer

----------------------------------------------------
-- CLEANUP GUARD
----------------------------------------------------

if _G.AdminPanelCleanup then
    pcall(_G.AdminPanelCleanup)
end

local ActiveConnections = {}

local function track(connection)
    table.insert(ActiveConnections, connection)
    return connection
end

_G.AdminPanelCleanup = function()
    for _, conn in ipairs(ActiveConnections) do
        pcall(function() conn:Disconnect() end)
    end
    ActiveConnections = {}

    pcall(function()
        if _G.AdminPanelStopFly then _G.AdminPanelStopFly() end
        if _G.AdminPanelStopNoclip then _G.AdminPanelStopNoclip() end
        if _G.AdminPanelStopESP then _G.AdminPanelStopESP() end
        if _G.AdminPanelClearBoulders then _G.AdminPanelClearBoulders() end
        if _G.AdminPanelStopAutoClicker then _G.AdminPanelStopAutoClicker() end
    end)

    pcall(function()
        local oldWindow = _G.AdminPanelWindow
        if oldWindow then
            pcall(function() oldWindow:Destroy() end)
        end
    end)
end

----------------------------------------------------
-- SAFE REMOTES LOADER
----------------------------------------------------

local RemotesFolder = ReplicatedStorage:FindFirstChild("Remotes")

local function getRemote(name)
    if RemotesFolder then
        return RemotesFolder:FindFirstChild(name)
    end
    return nil
end

local HoldComplete = getRemote("CrystalHoldComplete")
local DigEvent = getRemote("DigRequest")

task.spawn(function()
    if not RemotesFolder then
        RemotesFolder = ReplicatedStorage:WaitForChild("Remotes", 5)
        if RemotesFolder then
            HoldComplete = RemotesFolder:FindFirstChild("CrystalHoldComplete")
            DigEvent = RemotesFolder:FindFirstChild("DigRequest")
        end
    end
end)

----------------------------------------------------
-- HELPER & OPTIMIZED BACKPACK TRACKER
----------------------------------------------------

local PICK = {
    range = 18,
    cooldown = 0.1,
    restore = 0.2,
    burst = 5,
    retry = 0.2,
    forget = 4,
    pad = 3,
}

local CONTAINER_NAMES = { "DroppedCrystals", "Crystals", "Items", "Drops", "SpawnedItems" }
local rootPart = nil

local function bindCharacter(character)
    if not character then rootPart = nil return end
    rootPart = character:FindFirstChild("HumanoidRootPart")
end

bindCharacter(LocalPlayer.Character)
track(LocalPlayer.CharacterAdded:Connect(function(character)
    rootPart = nil
    task.wait(0.5)
    bindCharacter(character)
end))

local function getRoot()
    if rootPart and rootPart.Parent then return rootPart end
    bindCharacter(LocalPlayer.Character)
    return rootPart
end

local function getAttr(inst, name)
    local ok, value = pcall(inst.GetAttribute, inst, name)
    return ok and value or nil
end

local function ownsGamepass(name)
    local folder = LocalPlayer:FindFirstChild("GamepassesOwned")
    if not folder then return false end
    local flag = folder:FindFirstChild(name)
    return flag ~= nil and flag:IsA("BoolValue") and flag.Value == true
end

local function realStat(name)
    local data = LocalPlayer:FindFirstChild("PlayerData")
    local stats = data and data:FindFirstChild("RealStats")
    local entry = stats and stats:FindFirstChild(name)
    if not entry then return nil end
    return tonumber(entry.Value)
end

local function backpackCapacity()
    if LocalPlayer:GetAttribute("InfBackpack") == true then return math.huge end
    local base = realStat("CarryWeight") or 10
    if ownsGamepass("CarryKgPlus4") then base *= 4 end
    local total = base + (realStat("CarryWeightBonus") or 0)
    return total
end

local function backpackWeight()
    local total = 0
    local function scan(container)
        if not container then return end
        for _, child in ipairs(container:GetChildren()) do
            if child:IsA("Tool") and getAttr(child, "Tier") ~= nil then
                local kg = tonumber(getAttr(child, "WeightKg"))
                if kg then total += kg end
            end
        end
    end
    scan(LocalPlayer:FindFirstChildOfClass("Backpack"))
    scan(LocalPlayer.Character)
    return total
end

local function backpackFree()
    local capacity = backpackCapacity()
    if capacity == math.huge then return math.huge end
    return capacity - backpackWeight()
end

local function isValidPickable(inst)
    if not inst or not inst.Parent then return false end
    if inst:IsDescendantOf(LocalPlayer.Character) then return false end

    if getAttr(inst, "Collected") == true or getAttr(inst, "Picked") == true then 
        return false 
    end

    local prompt = inst:FindFirstChildOfClass("ProximityPrompt")
    local detector = inst:FindFirstChildOfClass("ClickDetector")

    if prompt or detector then
        return true
    end

    if inst:IsA("BasePart") then
        local attributes = inst:GetAttributes()
        if next(attributes) ~= nil then
            return true
        end
    end

    return false
end

local containerList = {}
local containerClock = 0

local function rebuildContainers()
    table.clear(containerList)
    local seen = {}
    local function push(container)
        if not container or seen[container] then return end
        seen[container] = true
        containerList[#containerList + 1] = container
    end
    push(Workspace)
    for _, name in ipairs(CONTAINER_NAMES) do push(Workspace:FindFirstChild(name)) end
    local things = Workspace:FindFirstChild("Things")
    if things then
        for _, name in ipairs(CONTAINER_NAMES) do push(things:FindFirstChild(name)) end
    end
end

local function eachContainer(fn)
    local now = os.clock()
    if #containerList == 0 or now - containerClock >= 2 then
        containerClock = now
        rebuildContainers()
    end
    for _, container in ipairs(containerList) do fn(container) end
end

----------------------------------------------------
-- AUTO PICKUP ENGINE
----------------------------------------------------

local pendingActions = {}
local promptRestores = {}
local claimed = {}
local promptCache = setmetatable({}, { __mode = "k" })

local function schedule(delay, fn)
    table.insert(pendingActions, { at = os.clock() + delay, fn = fn })
end

local function itemPrompt(inst)
    local cached = promptCache[inst]
    if cached and cached.Parent then return cached end
    local prompt = inst:FindFirstChildOfClass("ProximityPrompt")
    if prompt then
        promptCache[inst] = prompt
        return prompt
    end
    promptCache[inst] = nil
    return nil
end

local function surfaceDistance(part, origin)
    if part:IsA("Model") then
        local pos = part.PrimaryPart and part.PrimaryPart.Position or part:GetPivot().Position
        return (pos - origin).Magnitude
    end
    return (part.Position - origin).Magnitude
end

local function firePrompt(prompt)
    if not prompt or not prompt.Parent then return false end
    if not promptRestores[prompt] then
        promptRestores[prompt] = {
            hold = prompt.HoldDuration,
            sight = prompt.RequiresLineOfSight,
            enabled = prompt.Enabled,
            range = prompt.MaxActivationDistance,
        }
    end

    pcall(function()
        prompt.HoldDuration = 0
        prompt.RequiresLineOfSight = false
        prompt.Enabled = true
        prompt.MaxActivationDistance = 1000
    end)

    local fired = false
    if typeof(fireproximityprompt) == "function" then
        fired = pcall(fireproximityprompt, prompt, 1)
        if not fired then fired = pcall(fireproximityprompt, prompt) end
    end

    if not fired then
        pcall(function()
            prompt:InputHoldBegin()
            prompt:InputHoldEnd()
        end)
    end

    schedule(PICK.restore, function()
        local saved = promptRestores[prompt]
        if saved and prompt.Parent then
            prompt.HoldDuration = saved.hold
            prompt.RequiresLineOfSight = saved.sight
            prompt.Enabled = saved.enabled
            prompt.MaxActivationDistance = saved.range
        end
        promptRestores[prompt] = nil
    end)

    return fired
end

local pickupParams = OverlapParams.new()
pickupParams.FilterType = Enum.RaycastFilterType.Exclude

local pickupFound = {}
local pickupSeen = {}

local function pickupCandidates(free, origin)
    local found = pickupFound
    local seen = pickupSeen
    table.clear(found)
    table.clear(seen)
    local now = os.clock()

    local function consider(child)
        if not child or seen[child] then return end
        seen[child] = true

        if not child.Parent or not isValidPickable(child) then return end

        local claim = claimed[child]
        if claim and now - claim < PICK.retry then return end

        local weight = tonumber(getAttr(child, "WeightKg")) or 0
        if weight > free and free ~= math.huge then return end

        local distance = surfaceDistance(child, origin)
        if distance > PICK.range then return end

        found[#found + 1] = {
            inst = child,
            prompt = itemPrompt(child),
            weight = weight,
            distance = distance,
        }
    end

    pickupParams.FilterDescendantsInstances = { LocalPlayer.Character or LocalPlayer }

    local ok, hits = pcall(function()
        return Workspace:GetPartBoundsInRadius(origin, PICK.range + PICK.pad, pickupParams)
    end)

    if ok and hits then
        for _, part in ipairs(hits) do 
            consider(part) 
            if part.Parent and part.Parent:IsA("Model") then
                consider(part.Parent)
            end
        end
    end

    table.sort(found, function(a, b)
        return a.distance < b.distance
    end)

    return found
end

local function grabItem(inst, prompt)
    local sent = false

    if HoldComplete then
        sent = pcall(function() HoldComplete:FireServer(inst) end)
    end

    if not prompt then prompt = itemPrompt(inst) end

    if prompt and prompt.Parent and firePrompt(prompt) then sent = true end

    if not sent and typeof(fireclickdetector) == "function" then
        local detector = inst:FindFirstChildOfClass("ClickDetector")
        if detector then sent = pcall(fireclickdetector, detector, 0) end
    end

    return sent
end

local autoPickupActive = false
local autoPickupKeybind = Enum.KeyCode.G -- DEFAULT KEYBIND 'G'
local AutoPickupToggleRef = nil
local lastPickup = 0

local function pickupStep()
    local now = os.clock()
    if now - lastPickup < PICK.cooldown then return end

    local root = getRoot()
    if not root then return end

    local free = backpackFree()
    if free <= 0 then return end

    for inst, stamp in pairs(claimed) do
        if now - stamp >= PICK.forget or not inst.Parent then claimed[inst] = nil end
    end

    local candidates = pickupCandidates(free, root.Position)
    if #candidates == 0 then return end

    local budget = free
    local grabs = 0

    for _, entry in ipairs(candidates) do
        if grabs >= PICK.burst then break end
        if entry.weight <= budget or budget == math.huge then
            claimed[entry.inst] = now
            if grabItem(entry.inst, entry.prompt) then
                if budget ~= math.huge then budget -= entry.weight end
                grabs += 1
            end
        end
    end

    if grabs > 0 then lastPickup = now end
end

task.spawn(function()
    while true do
        task.wait(PICK.cooldown)
        if autoPickupActive then
            pcall(pickupStep)
        end

        if #pendingActions > 0 then
            local now = os.clock()
            for index = #pendingActions, 1, -1 do
                local job = pendingActions[index]
                if now >= job.at then
                    table.remove(pendingActions, index)
                    pcall(job.fn)
                end
            end
        end
    end
end)

----------------------------------------------------
-- LOAD WINDUI
----------------------------------------------------

local WindUISuccess, WindUI = pcall(function()
    return loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
end)

if not WindUISuccess or not WindUI then
    warn("Gagal memuat WindUI Library")
    return
end

_G.AdminPanelWindUI = WindUI

local Window = WindUI:CreateWindow({
    Title = "Zayn Panel",
    Icon = "rocket",
    Author = "by Zayn",
    Theme = "Dark",
})
_G.AdminPanelWindow = Window

----------------------------------------------------
-- STATE & ORIGINAL ZAYN PANEL FEATURES
----------------------------------------------------

local FlyEnabled = false
local FlySpeed = 120 -- DEFAULT SPEED 120
local WalkSpeedValue = 16
local JumpPowerValue = 50
local InfiniteJumpEnabled = false
local NoclipEnabled = false
local FlyKeybind = Enum.KeyCode.F -- DEFAULT KEYBIND 'F'
local CheckpointSlots = {}
local AutoTeleportOnRespawn = false
local AutoTeleportSlot = nil
local ESPEnabled = false

local FlyConnection = nil
local FlyBodyVelocity = nil
local FlyBodyGyro = nil
local InfiniteJumpConnection = nil
local NoclipConnection = nil

-- Fitur Ultra Auto Clicker
local autoClickerEnabled = false
local autoClickerClicksPerFrame = 2
local autoClickerLoop = nil
local autoClickerKeybind = nil
local AutoClickerToggleRef = nil

-- Fitur Mining
local turboDigEnabled = false
local turboDigLoop = nil
local turboDigKeybind = Enum.KeyCode.V -- DEFAULT KEYBIND 'V'
local TurboDigToggleRef = nil

local autoAimBoulder = false
local autoDigNoClick = false
local comboDigAimKeybind = Enum.KeyCode.X -- DEFAULT COMBO KEYBIND 'X'
local ComboToggleRef = nil
local AutoAimToggleRef = nil
local AutoDigToggleRef = nil

local instantPromptEnabled = false

-- Fitur Boulder Highlight & ESP Name
local voltiteHighlightEnabled = false
local voltiteHighlightCache = {}
local voltiteKeybind = Enum.KeyCode.C -- DEFAULT KEYBIND 'C'
local VoltiteToggleRef = nil

----------------------------------------------------
-- HELPER FUNCTIONS ORIGINAL
----------------------------------------------------

local function getCharacterParts()
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoid = character:WaitForChild("Humanoid")
    local rootPart = character:WaitForChild("HumanoidRootPart")
    return character, humanoid, rootPart
end

local function GetNearestBoulderPosition()
    local targetPath = workspace:FindFirstChild("MountainDecorations")
    targetPath = targetPath and targetPath:FindFirstChild("Boulders")
    if not targetPath then return nil end

    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end

    local nearestPos = nil
    local shortestDist = 50

    for _, obj in ipairs(targetPath:GetChildren()) do
        if obj:IsA("Model") or obj:IsA("BasePart") then
            local pos = obj:IsA("Model") and (obj.PrimaryPart and obj.PrimaryPart.Position or obj:GetPivot().Position) or obj.Position
            local dist = (hrp.Position - pos).Magnitude
            if dist < shortestDist then
                shortestDist = dist
                nearestPos = pos
            end
        end
    end
    return nearestPos
end

----------------------------------------------------
-- TURBO DIG TOGGLE FUNCTION
----------------------------------------------------

local function setTurboDig(state)
    turboDigEnabled = state
    if TurboDigToggleRef and TurboDigToggleRef.Set then
        TurboDigToggleRef:Set(state)
    end

    if turboDigLoop then
        task.cancel(turboDigLoop)
        turboDigLoop = nil
    end

    if turboDigEnabled and DigEvent then
        local fastEvent = DigEvent
        local fastMouse = LocalPlayer:GetMouse()
        local fastUis = UserInputService
        local localPlayer = LocalPlayer
        local mouseLeft = Enum.UserInputType.MouseButton1

        turboDigLoop = task.spawn(function()
            while turboDigEnabled do
                task.wait(0.08)
                if autoDigNoClick or fastUis:IsMouseButtonPressed(mouseLeft) then
                    local character = localPlayer.Character
                    local currentTool = character and character:FindFirstChildOfClass("Tool")

                    if not (autoDigNoClick and not currentTool) then
                        local dynamicItemName = currentTool and currentTool.Name or "Celestial Apex"
                        local targetPos = autoAimBoulder and GetNearestBoulderPosition() or (fastMouse.Hit and fastMouse.Hit.Position)

                        if targetPos then
                            fastEvent:FireServer(dynamicItemName, targetPos)
                        end
                    end
                end
            end
        end)
    end
end

----------------------------------------------------
-- ULTRA AUTO CLICKER LOGIC
----------------------------------------------------

local function toggleAutoClicker(state)
    autoClickerEnabled = state

    if autoClickerLoop then
        task.cancel(autoClickerLoop)
        autoClickerLoop = nil
    end

    if autoClickerEnabled then
        autoClickerLoop = task.spawn(function()
            while autoClickerEnabled do
                task.wait(0.05)
                local mousePos = UserInputService:GetMouseLocation()
                for i = 1, autoClickerClicksPerFrame do
                    VirtualInputManager:SendMouseButtonEvent(mousePos.X, mousePos.Y, 0, true, game, 1)
                    VirtualInputManager:SendMouseButtonEvent(mousePos.X, mousePos.Y, 0, false, game, 1)
                end
            end
        end)
    end
end

_G.AdminPanelStopAutoClicker = function()
    toggleAutoClicker(false)
end

----------------------------------------------------
-- BOULDERS HIGHLIGHT & NAME TAG ESP
----------------------------------------------------

local function ClearVoltiteHighlight()
    for obj, cache in pairs(voltiteHighlightCache) do
        pcall(function()
            if cache.Highlight then cache.Highlight:Destroy() end
            if cache.Billboard then cache.Billboard:Destroy() end
        end)
    end
    table.clear(voltiteHighlightCache)
end
_G.AdminPanelClearBoulders = ClearVoltiteHighlight

local function ForceMakeBoulderVisible(boulder)
    if not boulder then return end
    if boulder:IsA("BasePart") then
        boulder.Transparency = 0
    elseif boulder:IsA("Model") then
        for _, child in ipairs(boulder:GetDescendants()) do
            if child:IsA("BasePart") then child.Transparency = 0 end
        end
    end
end

task.spawn(function()
    while true do
        task.wait(0.5)
        if voltiteHighlightEnabled then
            pcall(function()
                local targetPath = workspace:FindFirstChild("MountainDecorations")
                targetPath = targetPath and targetPath:FindFirstChild("Boulders")
                
                if not targetPath then 
                    ClearVoltiteHighlight()
                    return 
                end
                
                local allBoulders = targetPath:GetChildren()
                local activeObjects = {}
                
                for _, obj in ipairs(allBoulders) do
                    if obj:IsA("Model") or obj:IsA("BasePart") then
                        ForceMakeBoulderVisible(obj)
                        activeObjects[obj] = true

                        if not voltiteHighlightCache[obj] then
                            local hl = Instance.new("Highlight")
                            hl.Name = "BouldersHighlight"
                            hl.Adornee = obj
                            hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                            hl.FillColor = Color3.fromRGB(0, 255, 255)
                            hl.FillTransparency = 0.35
                            hl.OutlineColor = Color3.fromRGB(255, 255, 255)
                            hl.OutlineTransparency = 0
                            hl.Parent = CoreGui

                            local bb = Instance.new("BillboardGui")
                            bb.Name = "BoulderNameTag"
                            bb.Adornee = obj
                            bb.Size = UDim2.new(0, 120, 0, 25)
                            bb.StudsOffset = Vector3.new(0, 3, 0)
                            bb.AlwaysOnTop = true
                            bb.Parent = CoreGui

                            local txt = Instance.new("TextLabel")
                            txt.Size = UDim2.new(1, 0, 1, 0)
                            txt.BackgroundTransparency = 1
                            txt.Text = "🪨 " .. obj.Name
                            txt.TextColor3 = Color3.fromRGB(0, 255, 255)
                            txt.TextStrokeTransparency = 0
                            txt.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                            txt.Font = Enum.Font.GothamBold
                            txt.TextSize = 12
                            txt.Parent = bb

                            voltiteHighlightCache[obj] = { Highlight = hl, Billboard = bb }
                        end
                    end
                end

                for obj, cache in pairs(voltiteHighlightCache) do
                    if not activeObjects[obj] or not obj.Parent then
                        pcall(function()
                            if cache.Highlight then cache.Highlight:Destroy() end
                            if cache.Billboard then cache.Billboard:Destroy() end
                        end)
                        voltiteHighlightCache[obj] = nil
                    end
                end
            end)
        end
    end
end)

----------------------------------------------------
-- INSTANT PROMPT LOGIC
----------------------------------------------------

track(ProximityPromptService.PromptShown:Connect(function(prompt)
    if instantPromptEnabled then
        local character = LocalPlayer.Character
        local hrp = character and character:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        local parentPart = prompt.Parent
        local promptPos = parentPart and (parentPart:IsA("BasePart") and parentPart.Position or parentPart:IsA("Model") and parentPart:GetPivot().Position)

        if promptPos and (hrp.Position - promptPos).Magnitude <= 12 then
            prompt.HoldDuration = 0
        end
    end
end))

track(ProximityPromptService.PromptButtonHoldBegan:Connect(function(prompt)
    if instantPromptEnabled then pcall(function() prompt:InputHoldEnd() end) end
end))

----------------------------------------------------
-- FLY FUNCTION
----------------------------------------------------

local function startFly()
    local character, humanoid, rootPart = getCharacterParts()

    FlyBodyVelocity = Instance.new("BodyVelocity")
    FlyBodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    FlyBodyVelocity.Velocity = Vector3.new(0, 0, 0)
    FlyBodyVelocity.Parent = rootPart

    FlyBodyGyro = Instance.new("BodyGyro")
    FlyBodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    FlyBodyGyro.P = 9000
    FlyBodyGyro.CFrame = rootPart.CFrame
    FlyBodyGyro.Parent = rootPart

    humanoid.PlatformStand = true

    FlyConnection = RunService.RenderStepped:Connect(function()
        local camera = workspace.CurrentCamera
        local moveDirection = Vector3.new(0, 0, 0)
        local camCFrame = camera.CFrame

        if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDirection += camCFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDirection -= camCFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDirection -= camCFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDirection += camCFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDirection += Vector3.new(0, 1, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then moveDirection -= Vector3.new(0, 1, 0) end

        if moveDirection.Magnitude > 0 then moveDirection = moveDirection.Unit end

        FlyBodyVelocity.Velocity = moveDirection * FlySpeed
        FlyBodyGyro.CFrame = camCFrame
    end)
end

local function stopFly()
    if FlyConnection then FlyConnection:Disconnect() FlyConnection = nil end
    if FlyBodyVelocity then FlyBodyVelocity:Destroy() FlyBodyVelocity = nil end
    if FlyBodyGyro then FlyBodyGyro:Destroy() FlyBodyGyro = nil end

    local character = LocalPlayer.Character
    if character then
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid.PlatformStand = false end
    end
end

_G.AdminPanelStopFly = stopFly

track(LocalPlayer.CharacterAdded:Connect(function()
    if FlyEnabled then task.wait(1) startFly() end
end))

----------------------------------------------------
-- WALKSPEED / JUMP POWER / INFINITE JUMP / NOCLIP
----------------------------------------------------

local function applyWalkSpeed(value)
    local character = LocalPlayer.Character
    if character then
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid.WalkSpeed = value end
    end
end
track(LocalPlayer.CharacterAdded:Connect(function() task.wait(1) applyWalkSpeed(WalkSpeedValue) end))

local function applyJumpPower(value)
    local character = LocalPlayer.Character
    if character then
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid.UseJumpPower = true humanoid.JumpPower = value end
    end
end
track(LocalPlayer.CharacterAdded:Connect(function() task.wait(1) applyJumpPower(JumpPowerValue) end))

local function startInfiniteJump()
    if InfiniteJumpConnection then return end
    InfiniteJumpConnection = UserInputService.JumpRequest:Connect(function()
        local character = LocalPlayer.Character
        local humanoid = character and character:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid:ChangeState(Enum.HumanoidStateType.Jumping) end
    end)
end

local function stopInfiniteJump()
    if InfiniteJumpConnection then InfiniteJumpConnection:Disconnect() InfiniteJumpConnection = nil end
end

local function startNoclip()
    if NoclipConnection then return end
    NoclipConnection = RunService.Stepped:Connect(function()
        local character = LocalPlayer.Character
        if not character then return end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") and part.CanCollide then part.CanCollide = false end
        end
    end)
end

local function stopNoclip()
    if NoclipConnection then NoclipConnection:Disconnect() NoclipConnection = nil end
    local character = LocalPlayer.Character
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = true end
        end
    end
end
_G.AdminPanelStopNoclip = stopNoclip

----------------------------------------------------
-- CHECKPOINT LOGIC & VISUALS
----------------------------------------------------

local SlotStatusLabels = {}

local function updateSlotUI(slot)
    if not SlotStatusLabels[slot] then return end
    
    if CheckpointSlots[slot] then
        local pos = CheckpointSlots[slot].Position
        local posText = string.format("X: %.1f | Y: %.1f | Z: %.1f", pos.X, pos.Y, pos.Z)
        SlotStatusLabels[slot]:SetTitle("🟢 Slot " .. slot)
        SlotStatusLabels[slot]:SetDesc("📍 Lokasi: " .. posText)
    else
        SlotStatusLabels[slot]:SetTitle("🔴 Slot " .. slot)
        SlotStatusLabels[slot]:SetDesc("Belum ada koordinat yang disimpan.")
    end
end

local function saveCheckpoint(slot)
    local character = LocalPlayer.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    if rootPart then 
        CheckpointSlots[slot] = rootPart.CFrame
        updateSlotUI(slot)
        return true 
    end
    return false
end

local function teleportToCheckpoint(slot)
    if not CheckpointSlots[slot] then
        WindUI:Notify({ Title = "Checkpoint", Content = "Slot " .. slot .. " masih kosong.", Duration = 3 })
        return
    end
    local character = LocalPlayer.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    if rootPart then rootPart.CFrame = CheckpointSlots[slot] end
end

local function deleteCheckpoint(slot)
    CheckpointSlots[slot] = nil
    if AutoTeleportSlot == slot then AutoTeleportSlot = nil AutoTeleportOnRespawn = false end
    updateSlotUI(slot)
end

track(LocalPlayer.CharacterAdded:Connect(function(character)
    if AutoTeleportOnRespawn and AutoTeleportSlot and CheckpointSlots[AutoTeleportSlot] then
        local rootPart = character:WaitForChild("HumanoidRootPart")
        task.wait(0.5)
        rootPart.CFrame = CheckpointSlots[AutoTeleportSlot]
    end
end))

----------------------------------------------------
-- ESP LOKAL PLAYER
----------------------------------------------------

local ESPCache = {}

local function createESP(player)
    if player == LocalPlayer or ESPCache[player] or not player.Character then return end
    local character = player.Character

    local highlight = Instance.new("Highlight")
    highlight.FillColor = Color3.fromRGB(0, 170, 255)
    highlight.FillTransparency = 0.6
    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
    highlight.OutlineTransparency = 0.2
    highlight.Parent = character

    local head = character:FindFirstChild("Head")
    local billboard

    if head then
        billboard = Instance.new("BillboardGui")
        billboard.Size = UDim2.new(0, 130, 0, 36)
        billboard.StudsOffset = Vector3.new(0, 2.5, 0)
        billboard.AlwaysOnTop = true
        billboard.Parent = head

        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(1, 0, 1, 0)
        frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
        frame.BackgroundTransparency = 0.2
        frame.BorderSizePixel = 0
        frame.Parent = billboard

        local frameCorner = Instance.new("UICorner")
        frameCorner.CornerRadius = UDim.new(0, 6)
        frameCorner.Parent = frame

        local accentLine = Instance.new("Frame")
        accentLine.Size = UDim2.new(0, 4, 1, -4)
        accentLine.Position = UDim2.new(0, 2, 0, 2)
        accentLine.BackgroundColor3 = Color3.fromRGB(0, 140, 255)
        accentLine.BorderSizePixel = 0
        accentLine.Parent = frame

        local lineCorner = Instance.new("UICorner")
        lineCorner.CornerRadius = UDim.new(0, 2)
        lineCorner.Parent = accentLine

        local nameLabel = Instance.new("TextLabel")
        nameLabel.Size = UDim2.new(1, -14, 0, 18)
        nameLabel.Position = UDim2.new(0, 10, 0, 2)
        nameLabel.BackgroundTransparency = 1
        nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        nameLabel.Font = Enum.Font.GothamBold
        nameLabel.TextSize = 13
        nameLabel.TextXAlignment = Enum.TextXAlignment.Left
        nameLabel.Text = player.Name
        nameLabel.Parent = frame

        local infoLabel = Instance.new("TextLabel")
        infoLabel.Size = UDim2.new(1, -14, 0, 14)
        infoLabel.Position = UDim2.new(0, 10, 0, 18)
        infoLabel.BackgroundTransparency = 1
        infoLabel.TextColor3 = Color3.fromRGB(160, 160, 160)
        infoLabel.Font = Enum.Font.Gotham
        infoLabel.TextSize = 10
        infoLabel.TextXAlignment = Enum.TextXAlignment.Left
        infoLabel.Text = "0m · Alt: 0m"
        infoLabel.Parent = frame

        local espTask = task.spawn(function()
            while ESPCache[player] do
                task.wait(0.2)
                local myChar = LocalPlayer.Character
                local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                local targetHrp = character and character:FindFirstChild("HumanoidRootPart")

                if myHrp and targetHrp then
                    local distance = math.floor((myHrp.Position - targetHrp.Position).Magnitude)
                    local altitude = math.floor(targetHrp.Position.Y)
                    infoLabel.Text = string.format("%dm  ·  Alt: %dm", distance, altitude)
                end
            end
        end)
    end

    ESPCache[player] = { Highlight = highlight, Billboard = billboard, Task = espTask }
end

local function removeESP(player)
    local cache = ESPCache[player]
    if cache then
        if cache.Task then task.cancel(cache.Task) end
        if cache.Highlight then cache.Highlight:Destroy() end
        if cache.Billboard then cache.Billboard:Destroy() end
        ESPCache[player] = nil
    end
end

local function startESP()
    for _, p in ipairs(Players:GetPlayers()) do createESP(p) end
end

local function stopESP()
    for p, _ in pairs(ESPCache) do removeESP(p) end
end

_G.AdminPanelStopESP = stopESP

local function setupESPTracking(player)
    if player == LocalPlayer then return end
    track(player.CharacterAdded:Connect(function()
        if ESPEnabled then
            removeESP(player)
            task.wait(0.5)
            createESP(player)
        end
    end))
end

for _, p in ipairs(Players:GetPlayers()) do setupESPTracking(p) end
track(Players.PlayerAdded:Connect(function(player)
    setupESPTracking(player)
    if ESPEnabled then
        player.CharacterAdded:Wait()
        task.wait(0.5)
        createESP(player)
    end
end))
track(Players.PlayerRemoving:Connect(removeESP))

----------------------------------------------------
-- MAIN TAB
----------------------------------------------------

local MainTab = Window:Tab({ Title = "Main", Icon = "house" })

local KEY_OPTIONS = {"None", "F", "G", "R", "T", "Y", "Z", "X", "C", "V", "B"}

local ToggleUIKeybind = Enum.KeyCode.R

MainTab:Dropdown({
    Title = "Toggle UI Keybind",
    Values = KEY_OPTIONS,
    Value = "R",
    Callback = function(keyName)
        ToggleUIKeybind = (keyName == "None" or not keyName) and nil or Enum.KeyCode[keyName]
    end
})

MainTab:Dropdown({
    Title = "Fly Keybind",
    Values = KEY_OPTIONS,
    Value = "F", -- DEFAULT KEYBIND 'F'
    Callback = function(keyName)
        FlyKeybind = (keyName == "None" or not keyName) and nil or Enum.KeyCode[keyName]
    end
})

track(UserInputService.InputBegan:Connect(function(input, gameProcessedEvent)
    if gameProcessedEvent then return end
    if input.UserInputType ~= Enum.UserInputType.Keyboard then return end
    
    if ToggleUIKeybind and input.KeyCode == ToggleUIKeybind then
        pcall(function() Window:Toggle() end)
    end
    
    if FlyKeybind and input.KeyCode == FlyKeybind then
        FlyEnabled = not FlyEnabled
        if FlyEnabled then startFly() else stopFly() end
    end

    if autoPickupKeybind and input.KeyCode == autoPickupKeybind then
        autoPickupActive = not autoPickupActive
        if AutoPickupToggleRef and AutoPickupToggleRef.Set then 
            AutoPickupToggleRef:Set(autoPickupActive) 
        end
    end

    if turboDigKeybind and input.KeyCode == turboDigKeybind then
        setTurboDig(not turboDigEnabled)
    end

    if voltiteKeybind and input.KeyCode == voltiteKeybind then
        voltiteHighlightEnabled = not voltiteHighlightEnabled
        if VoltiteToggleRef and VoltiteToggleRef.Set then VoltiteToggleRef:Set(voltiteHighlightEnabled) end
        if not voltiteHighlightEnabled then ClearVoltiteHighlight() end
    end

    if comboDigAimKeybind and input.KeyCode == comboDigAimKeybind then
        local newState = not autoAimBoulder
        autoAimBoulder = newState
        autoDigNoClick = newState
        if AutoAimToggleRef and AutoAimToggleRef.Set then AutoAimToggleRef:Set(newState) end
        if AutoDigToggleRef and AutoDigToggleRef.Set then AutoDigToggleRef:Set(newState) end
        if ComboToggleRef and ComboToggleRef.Set then ComboToggleRef:Set(newState) end
    end
end))

MainTab:Slider({
    Title = "Fly Speed",
    Step = 5,
    Value = { Min = 10, Max = 1000, Default = 120 }, -- DEFAULT FLY SPEED 120
    Callback = function(v) FlySpeed = v end
})

MainTab:Slider({
    Title = "Walk Speed",
    Step = 1,
    Value = { Min = 16, Max = 300, Default = WalkSpeedValue },
    Callback = function(v) WalkSpeedValue = v applyWalkSpeed(v) end
})

MainTab:Button({
    Title = "Reset Walk Speed (16)",
    Callback = function() WalkSpeedValue = 16 applyWalkSpeed(16) end
})

MainTab:Slider({
    Title = "Jump Power",
    Step = 10,
    Value = { Min = 0, Max = 300, Default = JumpPowerValue },
    Callback = function(v) JumpPowerValue = v applyJumpPower(v) end
})

MainTab:Toggle({
    Title = "Infinite Jump",
    Value = false,
    Callback = function(v) InfiniteJumpEnabled = v if v then startInfiniteJump() else stopInfiniteJump() end end
})

MainTab:Toggle({
    Title = "Noclip",
    Value = false,
    Callback = function(v) NoclipEnabled = v if v then startNoclip() else stopNoclip() end end
})

----------------------------------------------------
-- CHECKPOINT TAB
----------------------------------------------------

local CheckpointTab = Window:Tab({ Title = "Checkpoint", Icon = "map-pin" })

for i = 1, 3 do
    CheckpointTab:Section({ Title = "── Slot " .. i .. " ──" })

    SlotStatusLabels[i] = CheckpointTab:Paragraph({
        Title = "🔴 Slot " .. i,
        Desc = "Belum ada koordinat yang disimpan."
    })

    CheckpointTab:Button({
        Title = "💾 Save Slot " .. i,
        Callback = function()
            if saveCheckpoint(i) then
                WindUI:Notify({ Title = "Checkpoint", Content = "Slot " .. i .. " berhasil disimpan!", Duration = 3 })
            end
        end
    })

    CheckpointTab:Button({
        Title = "➡️ Teleport ke Slot " .. i,
        Callback = function()
            teleportToCheckpoint(i)
        end
    })

    CheckpointTab:Button({
        Title = "🗑️ Hapus Slot " .. i,
        Callback = function()
            deleteCheckpoint(i)
            WindUI:Notify({ Title = "Checkpoint", Content = "Slot " .. i .. " berhasil dihapus.", Duration = 3 })
        end
    })
end

CheckpointTab:Section({ Title = "── Pengaturan Respawn ──" })

CheckpointTab:Dropdown({
    Title = "Slot Auto TP Saat Respawn",
    Values = {"None", "Slot 1", "Slot 2", "Slot 3"},
    Value = "None",
    Callback = function(choice)
        AutoTeleportSlot = (choice == "Slot 1" and 1) or (choice == "Slot 2" and 2) or (choice == "Slot 3" and 3) or nil
    end
})

CheckpointTab:Toggle({
    Title = "Auto TP ke Slot Saat Respawn",
    Value = false,
    Callback = function(v) AutoTeleportOnRespawn = v end
})

----------------------------------------------------
-- PLAYERS TAB
----------------------------------------------------

local PlayersTab = Window:Tab({ Title = "Players", Icon = "users" })

local selectedPlayerName = nil
local function getOtherPlayerNames()
    local names = {}
    for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then table.insert(names, p.Name) end end
    return names
end

local PlayerDropdown = PlayersTab:Dropdown({
    Title = "Select Player",
    Values = getOtherPlayerNames(),
    Callback = function(name) selectedPlayerName = name end
})

PlayersTab:Button({
    Title = "Refresh Player List",
    Callback = function() PlayerDropdown:Refresh(getOtherPlayerNames()) end
})

PlayersTab:Button({
    Title = "Teleport To Player",
    Callback = function()
        local targetPlayer = selectedPlayerName and Players:FindFirstChild(selectedPlayerName)
        local myRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        local targetRoot = targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart")
        if myRoot and targetRoot then
            myRoot.CFrame = targetRoot.CFrame + Vector3.new(0, 2, 0)
        else
            WindUI:Notify({ Title = "Teleport Failed", Content = "Pilih player & pastikan karakter ada.", Duration = 3 })
        end
    end
})

track(Players.PlayerAdded:Connect(function() task.wait(1) PlayerDropdown:Refresh(getOtherPlayerNames()) end))
track(Players.PlayerRemoving:Connect(function() task.wait(1) PlayerDropdown:Refresh(getOtherPlayerNames()) end))

PlayersTab:Button({
    Title = "Tendangan/Pukulan Maut (Hati Hati Bug)", 
    Callback = function() 
        pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/Natural-Disaster-Survival-the-REAL-drop-kick-206988"))() end)
    end 
})

PlayersTab:Toggle({
    Title = "ESP Player (Custom UI)",
    Value = false,
    Callback = function(Value)
        ESPEnabled = Value
        if Value then startESP() else stopESP() end
    end
})

PlayersTab:Button({
    Title = "Load Aimbot + ESP", 
    Callback = function() 
        pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Universal-Keyless-Advanced-Aimbot-And-Esp-Gui-90617"))() end)
    end 
})

PlayersTab:Button({
    Title = "All Emote", 
    Callback = function() 
        pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-7yd7-I-Emote-Script-48024"))() end)
    end 
})

PlayersTab:Button({
    Title = "Copy Character Script", 
    Callback = function() 
        pcall(function() loadstring(game:HttpGet('https://raw.githubusercontent.com/Nearastro/Nearastro/refs/heads/main/00CopyAvaFE.lua'))() end)
    end 
})

----------------------------------------------------
-- MINING GUNUNG TAB
----------------------------------------------------

local MountainTab = Window:Tab({ Title = "Mining Gunung", Icon = "pickaxe" })

MountainTab:Dropdown({
    Title = "🔮 Boulders ESP Keybind",
    Values = KEY_OPTIONS,
    Value = "C", -- DEFAULT KEYBIND 'C'
    Callback = function(keyName)
        voltiteKeybind = (keyName == "None" or not keyName) and nil or Enum.KeyCode[keyName]
    end
})

VoltiteToggleRef = MountainTab:Toggle({
    Title = "🔮 Boulders Highlight + Name Tag (ESP)",
    Value = false,
    Callback = function(state)
        voltiteHighlightEnabled = state
        if not state then ClearVoltiteHighlight() end
    end
})

MountainTab:Toggle({
    Title = "⚡ Instant Fire Prompt",
    Value = false,
    Callback = function(Value)
        instantPromptEnabled = Value
    end
})

MountainTab:Dropdown({
    Title = "🧲 Auto Pick Up Keybind",
    Values = KEY_OPTIONS,
    Value = "G", -- DEFAULT KEYBIND 'G'
    Callback = function(keyName)
        autoPickupKeybind = (keyName == "None" or not keyName) and nil or Enum.KeyCode[keyName]
    end
})

AutoPickupToggleRef = MountainTab:Toggle({
    Title = "🧲 Auto Pick Up All Items/Attributes",
    Value = false,
    Callback = function(Value)
        autoPickupActive = Value
    end
})

MountainTab:Section({ Title = "── Auto Dig & Auto Aim Settings ──" })

MountainTab:Dropdown({
    Title = "⚙️ Combo Auto Dig + Aim Keybind",
    Values = KEY_OPTIONS,
    Value = "X", -- DEFAULT KEYBIND 'X'
    Callback = function(keyName)
        comboDigAimKeybind = (keyName == "None" or not keyName) and nil or Enum.KeyCode[keyName]
    end
})

ComboToggleRef = MountainTab:Toggle({
    Title = "⚡ Combo Auto Dig & Auto Aim Boulder (Toggle X)",
    Value = false,
    Callback = function(Value)
        autoAimBoulder = Value
        autoDigNoClick = Value
        if AutoAimToggleRef and AutoAimToggleRef.Set then AutoAimToggleRef:Set(Value) end
        if AutoDigToggleRef and AutoDigToggleRef.Set then AutoDigToggleRef:Set(Value) end
    end
})

AutoAimToggleRef = MountainTab:Toggle({
    Title = "🧲 Auto-Aim Nearest Boulder",
    Value = false,
    Callback = function(Value)
        autoAimBoulder = Value
    end
})

AutoDigToggleRef = MountainTab:Toggle({
    Title = "🤖 Auto Dig (No Click Required)",
    Value = false,
    Callback = function(Value)
        autoDigNoClick = Value
    end
})

MountainTab:Dropdown({
    Title = "🔥 Turbo Dig Keybind",
    Values = KEY_OPTIONS,
    Value = "V", -- DEFAULT KEYBIND 'V'
    Callback = function(keyName)
        turboDigKeybind = (keyName == "None" or not keyName) and nil or Enum.KeyCode[keyName]
    end
})

TurboDigToggleRef = MountainTab:Toggle({
    Title = "🔥 Turbo Dig (Master Switch)",
    Value = false,
    Callback = function(Value)
        setTurboDig(Value)
    end
})

----------------------------------------------------
-- MISCELLANEOUS TAB
----------------------------------------------------

local MiscTab = Window:Tab({ Title = "Miscellaneous", Icon = "list" })

MiscTab:Dropdown({
    Title = "⚡ Auto Clicker Keybind",
    Values = KEY_OPTIONS,
    Value = "None",
    Callback = function(keyName)
        autoClickerKeybind = (keyName == "None" or not keyName) and nil or Enum.KeyCode[keyName]
    end
})

AutoClickerToggleRef = MiscTab:Toggle({
    Title = "🖱️ Ultra Auto Clicker",
    Value = false,
    Callback = function(Value)
        toggleAutoClicker(Value)
    end
})

MiscTab:Slider({
    Title = "🔥 Clicks Per Loop",
    Step = 1,
    Value = { Min = 1, Max = 15, Default = autoClickerClicksPerFrame },
    Callback = function(v)
        autoClickerClicksPerFrame = v
    end
})

----------------------------------------------------
-- DEVELOPER TAB
----------------------------------------------------

local DevTab = Window:Tab({ Title = "Developer Tab", Icon = "code" })

DevTab:Button({
    Title = "DarkDex", 
    Callback = function() 
        pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/perfectusmim1/darkdex/refs/heads/main/darkdexp"))() end)
    end 
})

----------------------------------------------------
-- INFO TAB
----------------------------------------------------

local InfoTab = Window:Tab({ Title = "Info", Icon = "info" })

InfoTab:Paragraph({ Title = "About", Desc = "Credits : Zayn Scripter Homies" })
InfoTab:Paragraph({ Title = "Fly Controls", Desc = "W/A/S/D untuk bergerak, Space untuk naik, Left Ctrl untuk turun. Keybind Default: F (Speed: 120)." })
InfoTab:Paragraph({ Title = "Keybind Mining", Desc = "G: Auto Pickup | V: Turbo Dig | C: Boulders ESP + Name | X: Combo Auto Dig & Aim" })
InfoTab:Paragraph({ Title = "Zayn Love Livy", Desc = "YANG BIKIN SCRIPT INI UDAH PUNYA CEWE NAMANYA LIVYYYYYYYYYY!!!!!!!!!!!!" })

----------------------------------------------------
-- AUTO SELECT TAB MAIN
----------------------------------------------------

task.spawn(function()
    task.wait(0.15)
    pcall(function()
        if MainTab and MainTab.Select then
            MainTab:Select()
        elseif Window and Window.SelectTab then
            Window:SelectTab(1)
        end
    end)
end)

WindUI:Notify({
    Title = "Zayn Panel",
    Content = "GUI Loaded Successfully!",
    Duration = 5,
})
